
export const EMBEDDING_MODEL_NAME = 'embedding-001';
export const GENERATIVE_MODEL_NAME = 'gemini-2.5-flash';

// Standard embedding dimensions for the embedding-001 model
export const EMBEDDING_DIMENSIONS = 768;

// Config for chunking the document
export const CHUNK_SIZE = 1000; // characters
export const CHUNK_OVERLAP = 200; // characters